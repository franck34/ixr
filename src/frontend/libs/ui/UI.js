import { UIMenuLogger } from './menu/Logger.js';

function UI( context ) {

    function init() {
        
        const menuLogger = new UIMenuLogger( context );

    }

    init();

}

export { UI }
